
Re-write ex1 except uses processes instead of threads.

Once again execute "show router arp" on each of the devices.

Rather than waiting for all processes to complete, print the results as they are returned
from the devices.

