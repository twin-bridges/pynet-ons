
Create a Nornir script that establishes a Netmiko direct connection to
each of the SR-OS devices (in a custom task).

Return the device's prompt back from the custom task.

Use the print_result() function in your main program to print out the
result from custom task (thus printing the device's prompt).

